machine:
  timezone: Asia/Tokyo

deployment:
  push_to_server:
    branch: master
    commands:
      - rsync -rltgoDvzO --no-p --del -e "ssh -p $PORT_NUM" src/public/ $USERNAME@$HOSTNAME:$DOCUMENT_ROOT
